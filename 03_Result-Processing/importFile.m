function log = importfile1(filename, dataLines)
    %IMPORTFILE1 Import data from a text file
    %  LOG = IMPORTFILE1(FILENAME) reads data from text file FILENAME for
    %  the default selection.  Returns the data as a string array.
    %
    %  LOG = IMPORTFILE1(FILE, DATALINES) reads data for the specified row
    %  interval(s) of text file FILENAME. Specify DATALINES as a positive
    %  scalar integer or a N-by-2 array of positive scalar integers for
    %  dis-contiguous row intervals.
    %
    %  Example:
    %  log = importfile1("/home/leonard/TUHH/PhD/PhD_Fisser/Simulator/psc-ns3/simulation-results/test/log.txt", [1, Inf]);
    %
    %  See also READMATRIX.
    %
    % Auto-generated by MATLAB on 14-Mar-2021 14:59:59

    %% Input handling

    % If dataLines is not specified, define defaults
    if nargin < 2
        dataLines = [1, Inf];
    end

    %% Set up the Import Options and import the data
    opts = delimitedTextImportOptions("NumVariables", 5);

    % Specify range and delimiter
    opts.DataLines = dataLines;
    opts.Delimiter = "\t";

    % Specify column names and types
    opts.VariableNames = ["NodeID0XPosition15YPosition3", "VarName2", "VarName3", "VarName4", "VarName5"];
    opts.VariableTypes = ["string", "string", "string", "string", "string"];

    % Specify file level properties
    opts.ExtraColumnsRule = "ignore";
    opts.EmptyLineRule = "read";

    % Specify variable properties
    opts = setvaropts(opts, ["NodeID0XPosition15YPosition3", "VarName2", "VarName3", "VarName4", "VarName5"], "WhitespaceRule", "preserve");
    opts = setvaropts(opts, ["NodeID0XPosition15YPosition3", "VarName2", "VarName3", "VarName4", "VarName5"], "EmptyFieldRule", "auto");

    % Import the data
    log = readmatrix(filename, opts);
end